import { RefreshTokenStorage } from './refresh-token-storage';

describe('RefreshTokenStorage', () => {
  it('should be defined', () => {
    expect(new RefreshTokenStorage()).toBeDefined();
  });
});
